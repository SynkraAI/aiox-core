# Session 2026-04-07

## Projeto
- **Nome:** Content Forge Ecosystem
- **INDEX.md:** `docs/projects/content-forge-ecosystem/INDEX.md`

## O que foi feito

### Auditoria do ecossistema (research)
- Investigação profunda de 5 formatos de brand token incompatíveis
- Mapeamento de overlap entre 10 squads de conteúdo
- Identificação de 26 capabilities únicas no ecossistema
- Análise de skills: pipelines, ilhas, duplicações

### Plano Content Forge (aprovado)
- Abordagem federation (não merge) — squads ficam intactos
- Tier 0: Brand Schema unificado
- Tier 1: Capability Map
- Tier 2: Content Forge orquestrador
- Usuário pediu que todo orquestrador tenha "forge" no nome

### Implementação Tier 0: Brand Schema
- `packages/brand-schema/` com schema.json, index.mjs, 7 adapters
- Fix bug no brand-loader.mjs (validava flat, YAMLs eram nested)
- 8 brands migrados para localização canônica
- `data/active-brand.yaml` criado (ponteiro → specta)
- CLI funcional: `validate`, `list`, `from-hex-color`

### Implementação Tier 1: Capability Map
- `data/capability-map.yaml` com 26 capabilities
- `packages/capability-map/` com findBest(), findAlternatives(), search
- CLI funcional: `find`, `list`, `search`

### Implementação Tier 2: Content Forge
- `skills/content-forge/SKILL.md` com fluxo completo (6 fases)
- `lib/classifier.mjs` — classifica demanda em tipo/volume/urgência
- `lib/planner.mjs` — consulta capability-map e gera plano
- `references/examples.md` — 4 exemplos de planos

### viral-squad brand-agnostic
- Removidas TODAS referências a "academiaLendaria" (~20 ocorrências)
- Renomeado "Gold" → "Primary" em ~12 arquivos (~100 substituições)
- Agentes agora usam `brandTheme` em vez de `academiaLendariaTheme`
- Componentes .tsx já eram genéricos (importam @/styles/tokens)

## Agente/Squad em uso
Claude Opus 4.6 (direto, sem squad)

## Arquivos para contexto (próximo Claude DEVE ler)
- `packages/brand-schema/index.mjs` — core do brand schema
- `data/capability-map.yaml` — mapa de capacidades
- `skills/content-forge/SKILL.md` — instruções do orquestrador
- `data/active-brand.yaml` — marca ativa atual
- `.claude/plans/curious-finding-curry.md` — plano aprovado
- `docs/projects/content-forge-ecosystem/INDEX.md` — estado do projeto

## Decisões tomadas
- Federation > Merge — squads não são fundidos, orquestrador roteia
- "forge" no nome de todo orquestrador — memória gravada
- Brand schema YAML do lp-generator como base canônica (mais completo)
- viral-squad usa `brandTheme` genérico + regra 8% universal (não "Gold")
- Specta como active-brand temporária (precisa criar marca Luiz Fosc)

## Próximo passo exato
1. Ler marca real do Luiz Fosc: `~/Dropbox/Dropbox Particular Fosc/+PROJETOS Palestras + Mágicas/LUIZFOSC/Marca LUIZFOSC/Marca + manual Luiz Fosc`
2. Ler exemplos visuais: `~/Dropbox/.../ARTES`
3. Ler preview de outra sessão: `docs/research/2026-04-07-FOSC-ai-brand-identity-tools/brand-visual-preview.html`
4. Criar `packages/brand-schema/brands/luiz-fosc.yaml`
5. Gerar imagens modelo para validação visual

## Arquivos modificados não commitados
Nenhum — tudo commitado (3eb429728)
